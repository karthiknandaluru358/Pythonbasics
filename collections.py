#collections=single varaible used to store multiple values
# List=[] ordered and changable. Duplicates Ok
# set={} unordered and immutable,but Add/remove Ok. No duplicates
# Tuple=() ordered and unchaneable. Duplicates Ok Faster.


fruits=["apple","banana","gauva","grapes"]
for i in fruits:
    print(i)
sorted=fruits.sort()
print(sorted)